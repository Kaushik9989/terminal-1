<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Parcel Unlock Status</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <style>
    body {
      background-color: #f8f9fa;
      font-family: "Poppins", sans-serif;
      padding: 30px;
    }
    .card {
      max-width: 600px;
      margin: 0 auto;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .status {
      font-weight: bold;
      font-size: 1.2rem;
    }
    .status.awaiting_pick {
      color: #0d6efd;
    }
    .status.picked {
      color: #198754;
    }
    .status.awaiting_drop {
      color: #ffc107;
    }
    .icon {
      font-size: 3rem;
    }
    .btn-home {
      margin-top: 20px;
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <div class="card p-4">
    <div class="text-center mb-3">
      <% if (parcel.status === "awaiting_pick") { %>
        <div class="icon text-primary">🔓</div>
        <h3>Locker Unlocked!</h3>
        <p>Your locker has been successfully unlocked. Please collect your parcel.</p>
      <% } else if (parcel.status === "picked") { %>
        <div class="icon text-success">✅</div>
        <h3>Parcel Picked Up!</h3>
        <p>You have successfully picked up your parcel. Thank you for using our locker!</p>
      <% } else if (parcel.status === "awaiting_drop") { %>
        <div class="icon text-warning">📦</div>
        <h3>Ready for Drop-off</h3>
        <p>Locker is ready to receive your parcel. Please place it securely inside.</p>
      <% } else { %>
        <div class="icon text-secondary">ℹ️</div>
        <h3>Parcel Status</h3>
        <p>Current status: <%= parcel.status %></p>
      <% } %>
    </div>

    <hr />

    <h5>Parcel Details</h5>
    <ul class="list-group mb-3">
      <li class="list-group-item"><strong>Locker ID:</strong> <%= parcel.lockerId %></li>
      <li class="list-group-item"><strong>Compartment ID:</strong> <%= parcel.compartmentId %></li>
      <li class="list-group-item"><strong>Sender:</strong> <%= parcel.senderPhone %></li>
      <li class="list-group-item"><strong>Receiver:</strong> <%= parcel.receiverPhone %></li>
      <li class="list-group-item">
        <strong>Status:</strong>
        <span class="status <%= parcel.status %>"><%= parcel.status.toUpperCase() %></span>
      </li>
    </ul>

    <div class="text-center">
      <a href="/mobileDashboard" class="btn btn-primary btn-home">Go Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
