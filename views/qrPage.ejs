<!DOCTYPE html>
<html lang="en">
<head>
<meta name="theme-color" content="#0f172a">
  <meta charset="UTF-8" />
  <title>Parcel Status</title>

  <!-- Non-zoomable on mobile -->
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Font Awesome (for icons you used) -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <style>
    :root{
      --card-radius: 20px;
    }

    html, body {
      height: 100%;
      background: #f4f6f8;
    }

    /* Full-height area to center the card */
    .page {
      min-height: 100vh;
      display: flex;
      align-items: center;      /* vertical center */
      justify-content: center;  /* horizontal center */
      padding: 16px;
    }

    .qr-card {
      background: #ffffff;
      border-radius: var(--card-radius);
      box-shadow: 0 12px 30px rgba(0,0,0,0.12);
      width: 100%;
      max-width: 520px;
      padding: 28px 20px;
      text-align: center;
      animation: fadeInUp 0.45s ease;
    }

    .qr-card h4 {
      font-weight: 650;
      margin-bottom: 0.75rem;
      color: #212529;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      font-size: 1.25rem;
    }

    /* QR image: cover about half of the screen, always square */
    .qr-img {
      width: 50vmin;       /* ~ half of the smaller viewport dimension */
      height: 50vmin;      /* keep it square */
      max-width: 460px;    /* prevent it getting too huge on desktops */
      max-height: 460px;
      min-width: 220px;    /* reasonable minimum on very small screens */
      min-height: 220px;
      border-radius: 14px;
      border: 1px solid #e5e7eb;
      padding: 10px;
      background: #fff;
      transition: transform 0.25s ease;
      object-fit: contain;
    }

    .qr-img:hover {
      transform: translateY(-2px);
    }

    .info-block {
      margin-top: 1rem;
      text-align: left;
      font-size: 0.95rem;
      color: #555;
      background: #f8f9fa;
      border-radius: 12px;
      padding: 12px 14px;
    }

    .info-row + .info-row { margin-top: 6px; }
    .info-block i { width: 18px; text-align: center; margin-right: 8px; color: #6c757d; }
    .info-block strong { color: #000; font-weight: 600; }

    .btn-group-stack {
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
      margin-top: 1.25rem;
    }

    .btn-custom {
      padding: 0.68rem 1.1rem;
      font-size: 0.98rem;
      border-radius: 10px;
      transition: transform 0.18s ease, opacity 0.18s ease;
    }

    .btn-custom i { margin-right: 8px; }
    .btn-custom:hover { transform: translateY(-1.5px); opacity: 0.92; }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(14px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* Small tweaks for very small screens */
    @media (max-width: 360px) {
      .qr-card { padding: 22px 14px; }
      .qr-img { min-width: 200px; min-height: 200px; }
    }
  </style>
</head>

<body>
  <!-- Navbar (kept as-is) -->
  <%- include('../partials/navbar') %>

  <!-- Centered page area -->
  <main class="page">
    <div class="qr-card">
      <h4>
        <i class="fas fa-box-open text-primary"></i>
        Parcel QR Code
      </h4>

      <!-- QR (about half screen, square, centered) -->
      <img src="<%= qrImage %>" alt="QR Code" class="qr-img" />

      <!-- Info -->
      <div class="info-block mt-3">
        <div class="info-row"><i class="fas fa-lock"></i> Locker: <strong><%= parcel.lockerId || 'N/A' %></strong></div>
        <div class="info-row"><i class="fas fa-cube"></i> Compartment: <strong><%= (parseInt(parcel.compartmentId) + 1) || 'N/A' %></strong></div>
        <div class="info-row"><i class="fas fa-key"></i> Access Code: <strong><%= parcel.accessCode %></strong></div>
      </div>

      <!-- Actions -->
      <div class="btn-group-stack">
        <a
          href="https://www.google.com/maps/dir/?api=1&destination=<%= parcel.lockerLat %>,<%= parcel.lockerLng %>"
          target="_blank"
          rel="noopener"
          class="btn btn-primary btn-custom"
        >
          <i class="fas fa-location-arrow"></i>
          Get Directions
        </a>
      </div>
    </div>
  </main>


</body>
</html>
